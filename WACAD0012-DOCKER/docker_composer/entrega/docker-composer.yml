version: "3.9" 
name: "wacad0012" 

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "4000:4000"

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend